---
import { Image } from 'astro:assets'
import dotsRectangle from '@/images/dots-rectangle.svg'

const currentPath = Astro.url.pathname
---

<section
  class:list={[
    'newsletter mt-auto bg-gray-200 text-center pt-12 px-4 xl:px-0',
    [currentPath === '/' ? 'dark:bg-slate-900' : 'dark:bg-slate-800'],
  ]}
>
  <div class="max-w-xl mx-auto">
    <h3 class="text-xl font-bold dark:text-gray-100">Join our newsletter</h3>
    <p class="dark:text-gray-200 mt-4">
      
    </p>
    <form action="#" class="flex flex-col sm:flex-row mt-8">
      <input
        type="text"
        name="email-newsletter"
        id="email-newsletter"
        placeholder="Your email address"
        class="z-50 block dark:bg-slate-700 dark:text-gray-100 flex-1 w-full rounded-md border-0 text-gray-900 shadow-sm sm:rounded-r-none placeholder:text-gray-400 focus:ring-1 focus:ring-inset focus:ring-indigo-600 p-4"
      />
      <button
        class="z-50 flex-none uppercase
         bg-orange-600 hover:bg-orange-700 focus:bg-orange-700
         text-white rounded-sm sm:rounded-l-none
         px-8 tracking-wide shadow-sm focus:outline-hidden
         w-full sm:w-auto mt-4 sm:mt-0 py-2
         border border-orange-600 hover:border-orange-700 focus:border-orange-700"
        ><span>Subscribe</span>
      </button>
    </form>
  </div>

  <div class="max-w-3xl mx-auto -mt-12">
    <Image src={dotsRectangle} alt="dots" />
  </div>
</section>
